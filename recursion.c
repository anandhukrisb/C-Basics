#include <stdio.h>

// void disp(int n) {
//     if(n == 0) return;

//     disp(n - 1);
//     printf("%d ", n);
// }

// int fact(int n) {
//     if(n > 1) {
//         return n * fact(n - 1);
//     }
//     else {
//         return 1;
//     }
// }

// int sum(int n) {
//     if(n > 0) {
//         return n + sum(n - 1);
//     }
// }

int main() {

    // BASICS

    // Q) Print Numbers from 1 to N using Recursion
        // (Example: Input: 5 → Output: 1 2 3 4 5)

    // disp(5);
    
    // Q) Find Factorial using Recursion

    // printf("%d ", fact(5));

    // Q) Find the Sum of First N Natural Numbers

        // (Input: 4 → Output: 10)
    
    // printf("%d", sum(4));

    // MEDIUM

    // Q) Check if a String is Palindrome (Recursively)
        // (Input: "madam" → Output: Yes)

    

    return 0;
}