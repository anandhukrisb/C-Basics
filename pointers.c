#include <stdio.h>

int main() {

    // BASICS
    
    // Q) Declare an int variable and a pointer to it. Print:

        // the variable value

        // its address

        // value using the pointer
        // (Hint: Use & and * operators)
    
    

    return 0;
}